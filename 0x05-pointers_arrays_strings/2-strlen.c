#include "main.h"
#include <string.h>

/**
 * _strlen - determines the length of a string
 * @s: string of which size to be determined
 * Return: length of string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
