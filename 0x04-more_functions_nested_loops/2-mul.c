#include "main.h"

/**
 * mul - multiply two numbers
 * @a: first number to be multiplied
 * @b: second number to be multiplied
 * Return: multiple
 */
int mul(int a, int b)
{
	int multiple;

	multiple = a * b;
	return (multiple);
}
